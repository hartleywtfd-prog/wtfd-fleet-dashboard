# Turnout Gear Asset Detail Fix - Version 14

The `/preview-turnout-gear` payload now exposes distinct `assetTag`, `serialNumber`, `manufacturer`, `model`, `size`, and `barcode` values when those fields are available from OperativeIQ. This prevents category text from being displayed as an asset tag and allows the dashboard asset detail drawer to display manufacturer data.
