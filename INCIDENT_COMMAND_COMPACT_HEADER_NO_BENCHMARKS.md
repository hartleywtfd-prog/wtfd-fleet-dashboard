# Incident Command compact-header update

Changes in this build:

- Removes the Benchmarks panel from the live command-board layout.
- Keeps benchmark state logic intact in JavaScript so existing incident data and exports are not corrupted.
- Reduces the height of the locked-incident information area.
- Tightens incident title, location, metadata, and action-button spacing.
- Allows the Building / Occupancy panel to use the available right-column space.
- Updates the command stylesheet cache version to `0.3.1`.
